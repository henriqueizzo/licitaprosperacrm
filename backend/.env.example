# Chave da API Anthropic (criar em https://console.anthropic.com -> API Keys)
ANTHROPIC_API_KEY=

# Chave da API do Google Gemini — GRATUITA (criar em https://aistudio.google.com/apikey)
# Se preenchida, a análise dos editais usa o Gemini sem custo.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Provedor da análise IA: gemini | anthropic | vazio (auto: usa Gemini se tiver
# chave, senão o Claude)
IA_PROVIDER=

# Token da API ConLicitação (solicitar ao suporte ConLicitação como assinante)
CONLICITACAO_TOKEN=

# Banco de dados (SQLite local por padrão; na nuvem use a URL do Postgres/Supabase,
# ex.: postgresql://usuario:senha@host:5432/postgres — formatos postgres:// e
# postgresql:// são normalizados automaticamente para o driver psycopg2)
DATABASE_URL=sqlite:///./licitaprospera.db

# Coleta automática a cada N horas (0 = desligado, use POST /api/pipeline/executar)
COLETA_INTERVALO_HORAS=6

# Score mínimo (0-100) para criar oportunidade automaticamente no CRM
SCORE_MINIMO_OPORTUNIDADE=60

# Admin inicial: criado automaticamente no 1º startup se não existir nenhum usuário.
# TROQUE A SENHA no primeiro acesso (menu "Trocar senha" no app).
ADMIN_EMAIL=admin@exemplo.com
ADMIN_SENHA_INICIAL=troque-me

# Cookie de sessão com Secure (exige HTTPS). Deixe false no PC local; true na nuvem.
COOKIE_SECURE=false

# Validade da sessão de login, em dias
SESSAO_DIAS=7

# Token do endpoint POST /api/pipeline/executar-cron (coleta disparada por cron
# externo, ex.: cron-job.org, enviando o header X-Cron-Token ou ?token=).
# Gere um valor longo e aleatório. Vazio = rota desabilitada (404).
CRON_TOKEN=

# Diretório do build do frontend (relativo à pasta backend). Se existir, o FastAPI
# serve o app React em produção no mesmo domínio da API. Em dev pode ignorar.
FRONTEND_DIST=../frontend/dist
